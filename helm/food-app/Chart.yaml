apiVersion: v2
name: food-app
description: Helm chart for the Food recipe application (backend, frontend, mongodb)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: velero
    version: "8.1.0"
    repository: "https://vmware-tanzu.github.io/helm-charts"
    condition: velero.enabled
